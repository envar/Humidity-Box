#ifndef _ENUMS_H
#define _ENUMS_H

enum ModeOption { LOOP, INIT, RET_YES, RET_NO, RET_EXEC };
enum SelectionOption { YES, NO, ADD, REMOVE, EXIT, START, END, HOUR, MIN, MONTH, DAY, YEAR, RH_SP };
enum LoopNum { LOOP1, LOOP2, LOOP3, LOOP4, LOOP5 };

#endif
